BveTs Map 2.02

// このファイルは、他の init_*.txt ファイルから一度だけ include される。
// 全てのマップに共通の初期化を行う。

$distance = 0;

Structure.Load('../structures/structures_' + floor(rand()) + '.csv');
$ballast_5m_count = 2;
